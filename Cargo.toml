[package]
name = "system"
version = "0.1.0"
edition = "2018"

[dependencies]
bootloader = "0.11.11"
volatile = "0.2.6"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[package.metadata.bootimage]
test-args = ["-device", "isa-debug-exit,iobase=0xf4,iosize=0x04"]
